<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================
         Statusbar – minimal footer rail for the LibraryView.
         Left:  game count
         Right: theme version + filter-active hint
         ============================================================ -->

    <ControlTemplate x:Key="StatusbarTemplate" TargetType="ContentControl">
        <Border Background="{DynamicResource BgPanelBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,6,14,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Style="{StaticResource CaptionTextStyle}"
                           VerticalAlignment="Center">
                    <Run Text="Games: " />
                    <Run Text="{Binding MainModel.GamesView.CollectionView.Count,
                                        Mode=OneWay,
                                        FallbackValue=0,
                                        TargetNullValue=0}" />
                </TextBlock>

                <TextBlock Grid.Column="1"
                           Style="{StaticResource CaptionTextStyle}"
                           Text="Filters active"
                           Foreground="{DynamicResource AccentBlueBrush}"
                           Margin="0,0,16,0"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding MainModel.AppSettings.FilterSettings.IsActive,
                                                               FallbackValue=False,
                                                               TargetNullValue=False}"
                                             Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Grid.Column="2"
                           Style="{StaticResource CaptionTextStyle}"
                           Text="SteamLike v0.1.0"
                           VerticalAlignment="Center" />
            </Grid>
        </Border>
    </ControlTemplate>

    <Style x:Key="StatusbarStyle" TargetType="ContentControl">
        <Setter Property="Template" Value="{StaticResource StatusbarTemplate}" />
    </Style>

</ResourceDictionary>
